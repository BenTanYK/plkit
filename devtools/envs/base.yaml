name: plkit

channels:
  - conda-forge

dependencies:

  - python >=3.10
  - pip

  - click

  # Dev / Testing
  - setuptools_scm >=8

  - pre-commit
  - ruff

  - numpy
  - pandas
  - openpyxl

  - pytest
  - pytest-mock
  - pytest-cov
  - codecov

  # Docs
  - mkdocs
  - mkdocs-material
  - mkdocs-gen-files
  - mkdocs-literate-nav
  - mkdocstrings
  - mkdocstrings-python >=1.10.8
  - black
  - mike
